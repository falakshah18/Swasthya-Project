
# Frontend

This is a clean frontend scaffold for GitHub.

The current UI in your uploaded project is still server-rendered with Django templates. To keep backend and frontend separate in the repo, the original HTML templates and static assets were copied here as migration references.

Reference files:
- `src/pages/*.html`
- `src/assets/css/*`
- `src/assets/js/*`
