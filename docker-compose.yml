
version: "3.9"

services:
  backend:
    build: ./backend
    container_name: swasthya-backend
    ports:
      - "8000:8000"
    env_file:
      - .env.example
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: swasthya-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped
